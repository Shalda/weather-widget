import { BuildMenuFilterPipe } from './build-menu-filter.pipe';

describe('BuildMenuFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildMenuFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
